# Data Notes

This project uses Hugging Face Datasets for loading text classification data.

Supported datasets in current code:

- `ag_news`
- `sst2` (GLUE/SST-2)

No manual download is required; datasets are fetched automatically by the scripts.
